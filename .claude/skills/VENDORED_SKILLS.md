# Vendored agent skill packs

These third-party skill packs are vendored into `.claude/skills/` (and mirrored as
per-skill symlinks under `.agents/skills/`) so Cursor / Claude Code / Codex agents working
in this repo can discover them. They are independent of Mole's own project skills (`bugs`,
`mole`, `release-flow`, `release-notes`).

Skills are laid out **one directory deep** (`.claude/skills/<skill>/SKILL.md`), matching
how the agent runtime discovers skills. Because packs share skill names (e.g. `tdd`,
`review`, `teach`), each pack's skills are namespaced with a prefix.

See also: [AGENT_SKILL_PACKS.md](AGENT_SKILL_PACKS.md) for install commands and curation notes.

| Prefix | Upstream | Author | License | Notes |
| --- | --- | --- | --- | --- |
| `gstack-*` | https://github.com/garrytan/gstack | Garry Tan | MIT | 53 skills; runtime via `scripts/install-gstack-cursor.sh` |
| `pstack-*` | https://github.com/cursor/plugins/tree/main/pstack | Lauren Tan (poteto) | MIT | 45 skills; native Cursor plugin preferred |
| `mattpocock-*` | https://github.com/mattpocock/skills | Matt Pocock | MIT | 37 skills |
| `trailofbits-*` | https://github.com/trailofbits/skills | Trail of Bits | MIT | Security / audit skills |
| `vercel-*` | https://github.com/vercel-labs/agent-skills | Vercel | MIT | React/Next/deploy patterns |
| `find-skills` | https://github.com/vercel-labs/skills | Vercel | MIT | Skill discovery |
| `agent-browser` | https://github.com/vercel-labs/agent-browser | Vercel | MIT | Playwright browser automation |
| `ce-*`, `lfg` | https://github.com/EveryInc/compound-engineering-plugin | Every | MIT | Compound engineering workflow |
| `shadcn-shadcn` | https://github.com/shadcn/ui | shadcn | MIT | UI component workflows |

The **superpowers** set (https://github.com/obra/superpowers) is installed separately via
`npx skills add obra/superpowers` / Cursor marketplace — intentionally not vendored here.

## Notes

- **Do not run gstack skills that rewrite `CLAUDE.md` in this repo.** In Mole,
  `CLAUDE.md` is a symlink to `AGENTS.md` (the project contract). Skills such as
  `gstack-sync-gbrain`, `gstack-ship`, and `gstack-review` that append routing or
  gbrain blocks to `CLAUDE.md` would mutate `AGENTS.md` instead. Use Mole-native
  skills (`bugs`, `mole`, `release-flow`) for project work here.
- `gstack-*` holds the Cursor-flavored skill docs generated by `gstack`'s
  `setup --host cursor`. Skills that shell out to gstack's browser tooling (`/browse`,
  `/qa`, `/make-pdf`, ...) expect the gstack runtime (Bun + Playwright browsers) to be
  installed; run `scripts/install-gstack-cursor.sh` in Cloud/local envs.
- `pstack-*` internal `../principle-*` cross-references were rewritten to the prefixed
  directory names so relative links resolve.
- To refresh portable packs: `./scripts/install-agent-skills.sh` then `./scripts/vendor-agent-skills.sh`.
